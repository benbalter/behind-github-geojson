title: "Open source, open standards and 50 lines of code"
description: "A look behind GitHub’s GeoJSON rendering and diffing"

collections:
  - slides

defaults:
-
  scope:
    type: "slides"
  values:
    title: ""

exclude:
  - script
  - ".gitignore"
  - Gemfile
  - Gemfile.lock
  - readme.md
  - node_modules
  - ".sass-cache"
  - "package.json"
  - src
  - "bower.json"

highlighter: "pygments"
excerpt_separator: ""

gems:
  - jekyll-mentions
  - jemoji

emoji:
  src: "images"

sass:
  style: :compressed
